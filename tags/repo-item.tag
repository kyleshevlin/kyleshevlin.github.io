<repo-item>
  <div class="repo-item">
    <h3><a href={ opts.repopath }>{ opts.title }</a></h3>
    <p>{ opts.desc }</p>
    <a if={ opts.demopath } href={ opts.demopath }>Demo</a>
  </div>
</repo-item>
